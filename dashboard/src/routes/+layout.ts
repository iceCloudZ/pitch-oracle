// The dashboard is a fully prerendered static site: every route is crawled at
// build time and emitted as plain HTML/JS. SSR stays on so prerender works.
export const prerender = true
export const ssr = true
